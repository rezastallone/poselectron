import React from 'react'
import { ProductListProp } from '../kasir/ProductListView'

export const ReceitView: React.FC<any & ProductListProp> = (props: any) => {

  const [cart] = props

  return (
    <div>

    </div>
  )
}
